# 🚀 Инструкция по запуску бота 24/7

## Проблема

GitHub Actions **только проверяет** токен, но **не запускает бота**.
Бот нужно запустить на сервере, чтобы он работал постоянно.

---

## ✅ Вариант 1: Heroku (Бесплатно, 24/7) - РЕКОМЕНДУЮ

### Шаг 1: Установите Heroku CLI

**macOS:**
```bash
brew tap heroku/brew && brew install heroku
```

**Linux:**
```bash
curl https://cli-assets.heroku.com/install.sh | sh
```

### Шаг 2: Войдите в Heroku
```bash
heroku login
```

### Шаг 3: Создайте приложение
```bash
cd /Users/grigoryvinogradov/PSS2/PoketonSS
heroku create poketon-ss-bot
```

### Шаг 4: Добавьте токен в Heroku
```bash
heroku config:set TSSLG=ваш_токен_от_BotFather
```

### Шаг 5: Задеплойте бота
```bash
git push heroku main
```

### Шаг 6: Запустите worker
```bash
heroku ps:scale worker=1
```

### Шаг 7: Проверьте логи
```bash
heroku logs --tail
```

**Готово!** Бот работает 24/7 на Heroku.

---

## ✅ Вариант 2: Railway (Бесплатно, 24/7)

### Шаг 1: Зайдите на Railway
Откройте: https://railway.app/

### Шаг 2: Подключите GitHub репозиторий
1. New Project → Deploy from GitHub repo
2. Выберите `Vingrig-Studio/PoketonSS`

### Шаг 3: Добавьте переменную окружения
1. Variables → New Variable
2. `TSSLG` = ваш_токен_от_BotFather

### Шаг 4: Деплой
Railway автоматически задеплоит бота.

**Готово!** Бот работает 24/7 на Railway.

---

## ✅ Вариант 3: Запуск локально (для тестирования)

```bash
cd /Users/grigoryvinogradov/PSS2/PoketonSS

# Создайте .env файл
echo "TSSLG=ваш_токен" > .env

# Запустите бота
python3 bot.py

# Остановить: Ctrl+C
# Удалить .env: rm .env
```

---

## 📊 Сравнение вариантов

| Вариант | Стоимость | Сложность | Время работы |
|---------|-----------|-----------|--------------|
| **Heroku** | Бесплатно* | Средняя | 24/7 |
| **Railway** | Бесплатно* | Легкая | 24/7 |
| **Локально** | Бесплатно | Очень легкая | Пока включен компьютер |

*Может требоваться кредитная карта для верификации

---

## 🎯 Что выбрать?

- **Heroku** - если умеете работать с командной строкой
- **Railway** - если хотите простой веб-интерфейс
- **Локально** - только для тестирования

---

## 🆘 Нужна помощь?

Напишите какой вариант выбрали, и я помогу с настройкой!

