# Telegram Bot для Sticker Shot

## 🚀 Быстрый старт

### 1. Создание бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Sticker Shot Bot")
   - Введите username бота (должен заканчиваться на "bot", например: "PoketonSSBot")
4. Сохраните токен, который даст вам BotFather

### 2. Установка зависимостей

```bash
# Установите Python 3.8 или выше, если еще не установлен

# Установите необходимые библиотеки
pip install -r requirements.txt
```

### 3. Настройка бота

Откройте файл `bot.py` и замените:
```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
```
на ваш токен от BotFather:
```python
BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
```

### 4. Запуск бота

```bash
python bot.py
```

Бот будет работать до тех пор, пока вы не остановите программу (Ctrl+C).

## 📝 Функционал

### Команда /start
При вызове команды `/start` бот:
- Отправляет изображение `TG.png`
- Добавляет текст на английском:
  > Time to improve the game together, leave your feedback about the game at https://t.me/vingrigstudio
  > 
  > Now press play Sticker Shot!!!
- Добавляет кнопку для запуска игры

### Команда /help
Показывает справочную информацию о доступных командах.

## 🔧 Дополнительные настройки

### Изменение текста сообщения
Отредактируйте переменную `MESSAGE_TEXT` в файле `bot.py`.

### Изменение пути к изображению
Если изображение находится в другой папке, измените переменную `IMAGE_PATH`.

### Изменение URL игры
В функции `start_command` найдите создание кнопки и измените URL:
```python
InlineKeyboardButton("🎮 Play Sticker Shot", url="ВАШ_URL")
```

## 🌐 Деплой на сервер (опционально)

Для круглосуточной работы бота рекомендуется развернуть его на сервере:

### Использование systemd (Linux)
Создайте файл `/etc/systemd/system/stickershot-bot.service`:
```ini
[Unit]
Description=Sticker Shot Telegram Bot
After=network.target

[Service]
Type=simple
User=YOUR_USER
WorkingDirectory=/path/to/PoketonSS
ExecStart=/usr/bin/python3 /path/to/PoketonSS/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable stickershot-bot
sudo systemctl start stickershot-bot
```

### Использование Docker
Создайте `Dockerfile`:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "bot.py"]
```

Запустите:
```bash
docker build -t stickershot-bot .
docker run -d --name stickershot-bot stickershot-bot
```

## 📚 Полезные ссылки

- [Документация python-telegram-bot](https://docs.python-telegram-bot.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Канал студии](https://t.me/vingrigstudio)

## 🐛 Решение проблем

### Ошибка "File not found"
Убедитесь, что файл `TG.png` находится в той же папке, что и `bot.py`.

### Ошибка "Unauthorized"
Проверьте правильность токена бота в файле `bot.py`.

### Бот не отвечает
- Проверьте подключение к интернету
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

