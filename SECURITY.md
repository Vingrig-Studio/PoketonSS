# 🔒 Безопасность и конфиденциальность

## ⚠️ ВАЖНО: Защита API ключей

Этот проект использует Telegram Bot API, который требует секретный токен. **НИКОГДА** не публикуйте свой токен в открытом доступе!

## 🔐 Безопасные способы хранения токена

Бот поддерживает **3 способа** безопасного хранения токена (в порядке приоритета):

### Способ 1: Переменная окружения (Рекомендуется для продакшена)

```bash
# Linux/macOS
export TELEGRAM_BOT_TOKEN="ваш_токен"
python3 bot.py

# Windows CMD
set TELEGRAM_BOT_TOKEN=ваш_токен
python bot.py

# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
python bot.py
```

**Плюсы:**
- ✅ Самый безопасный способ
- ✅ Токен не хранится в файлах
- ✅ Идеально для серверов и CI/CD

**Минусы:**
- ❌ Нужно устанавливать при каждом запуске терминала

**Для постоянного использования добавьте в:**
- Linux/macOS: `~/.bashrc` или `~/.zshrc`
- Windows: Системные переменные среды

### Способ 2: Файл .env (Рекомендуется для разработки)

1. Скопируйте файл-пример:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте токен:
```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

3. Запустите бота:
```bash
python3 bot.py
```

**Плюсы:**
- ✅ Удобно для локальной разработки
- ✅ Файл автоматически в `.gitignore`
- ✅ Не нужно устанавливать переменные

**Минусы:**
- ⚠️ Храните файл локально, не отправляйте никому

### Способ 3: Файл config.py (Альтернатива)

1. Скопируйте файл-пример:
```bash
cp config.example.py config.py
```

2. Откройте `config.py` и вставьте токен:
```python
TELEGRAM_BOT_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz'
```

3. Запустите бота:
```bash
python3 bot.py
```

**Плюсы:**
- ✅ Простой Python-файл
- ✅ Файл автоматически в `.gitignore`

**Минусы:**
- ⚠️ Храните файл локально, не отправляйте никому

## 🚨 Что делать, если токен попал в Git

Если вы **случайно** закоммитили токен в Git:

### 1. Немедленно отзовите токен:
- Откройте [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте команду `/mybots`
- Выберите своего бота
- Нажмите "API Token"
- Нажмите "Revoke current token"

### 2. Создайте новый токен:
- В том же меню нажмите "Generate new token"
- Обновите токен в вашем `.env` или `config.py`

### 3. Очистите историю Git (если нужно):
```bash
# ВНИМАНИЕ: Это изменит историю! Используйте с осторожностью
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch config.py" \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

## ✅ Проверка безопасности

Убедитесь, что эти файлы **НЕ** отслеживаются Git:

```bash
git status
```

Если вы видите `.env` или `config.py` в списке - **НЕ КОММИТЬТЕ ИХ!**

Файл `.gitignore` уже настроен и защищает:
- `.env`
- `config.py`
- `*.key`
- `secrets.json`

## 📚 Дополнительные рекомендации

1. **Разные токены для разработки и продакшена**
   - Создайте двух ботов: один для тестов, другой для пользователей

2. **Регулярно меняйте токены**
   - Обновляйте токен раз в несколько месяцев

3. **Не передавайте токен по незащищенным каналам**
   - Не отправляйте в обычных сообщениях
   - Не храните в облачных заметках без шифрования

4. **Используйте права доступа к файлам** (Linux/macOS):
```bash
chmod 600 .env
chmod 600 config.py
```

## 🆘 Нужна помощь?

Если у вас возникли вопросы по безопасности, обратитесь:
- [Канал студии](https://t.me/vingrigstudio)
- [Чат студии](https://t.me/chatvingrig)

---

**Помните:** Токен бота - это как пароль от вашего аккаунта. Храните его в безопасности! 🔐

