# 🚀 Быстрая инструкция по настройке

## Шаг 1: Получите токен бота от @BotFather

1. Откройте Telegram → найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Введите имя бота (например: `Sticker Shot Bot`)
4. Введите username бота (например: `PoketonSSBot` - должен заканчиваться на `bot`)
5. **Скопируйте токен** (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Шаг 2: Добавьте токен в GitHub Secrets

### Откройте настройки GitHub:

https://github.com/Vingrig-Studio/PoketonSS/settings/secrets/actions

### Добавьте секрет:

1. Нажмите **"New repository secret"**
2. **Name**: `TSSLG`
3. **Secret**: Вставьте ваш токен от @BotFather
4. Нажмите **"Add secret"**

✅ **Готово!** Токен безопасно сохранен в GitHub.

---

## Шаг 3: Что мне передать

**После добавления токена в GitHub Secrets, напишите мне:**

```
"Токен добавлен в GitHub Secrets"
```

**ВСЁ!** Больше ничего передавать не нужно.

❌ **НЕ ОТПРАВЛЯЙТЕ** мне сам токен  
❌ **НЕ ПУБЛИКУЙТЕ** токен в сообщениях  
✅ **ТОЛЬКО** подтвердите, что добавили его в GitHub Secrets

---

## Шаг 4: Запуск бота локально (опционально)

Если хотите запустить бота на своем компьютере для тестов:

```bash
cd /Users/grigoryvinogradov/PSS2/PoketonSS

# Создайте .env файл
echo "TSSLG=ваш_токен" > .env

# Установите зависимости
pip3 install -r requirements.txt

# Запустите бота
python3 bot.py
```

---

## 📖 Подробная документация

Если нужны детали, читайте:

- `GITHUB_SECRETS_GUIDE.md` - Подробно про GitHub Secrets
- `DEPLOY_SERVER_GUIDE.md` - Как запустить на сервере 24/7

---

## ✅ Что дальше?

После добавления токена в GitHub Secrets:

1. ✅ Токен защищен и зашифрован
2. ✅ Можно заливать код на GitHub (`git push`)
3. ✅ GitHub Actions автоматически будет работать с токеном
4. ✅ Токен НЕ попадет в код и историю Git
