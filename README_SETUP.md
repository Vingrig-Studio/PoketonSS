# 🤖 Telegram Bot Setup

## Работа через GitHub Secrets

Бот настроен для работы **исключительно через GitHub Secrets**. 

### ✅ Что настроено:

1. **GitHub Secret `TSSLG`** - содержит токен бота
2. **GitHub Actions** - автоматически проверяет и деплоит бота
3. **Код бота** - читает токен из переменной окружения `TSSLG`

### 🚀 Как это работает:

При каждом `git push` в `main`:
- GitHub Actions запускается автоматически
- Загружает токен из Secret `TSSLG`
- Проверяет конфигурацию бота
- Готов к деплою на сервер

### 🔒 Безопасность:

- ❌ Токен **НЕ хранится** в коде
- ❌ Токен **НЕ хранится** в репозитории
- ✅ Токен **ТОЛЬКО** в GitHub Secrets
- ✅ Файл `.env` в `.gitignore`

### 📋 GitHub Actions Workflow:

Файл: `.github/workflows/deploy.yml`

```yaml
env:
  TSSLG: ${{ secrets.TSSLG }}
```

### 🔗 Ссылки:

- **Secrets настройки:** https://github.com/Vingrig-Studio/PoketonSS/settings/secrets/actions
- **Actions логи:** https://github.com/Vingrig-Studio/PoketonSS/actions

---

## Локальное тестирование (опционально)

Если нужно протестировать локально, создайте временный `.env` файл:

```bash
echo "TSSLG=ваш_токен" > .env
python3 bot.py
# После тестирования удалите .env
rm .env
```

⚠️ **Важно:** Файл `.env` автоматически игнорируется Git и не попадет в репозиторий.

