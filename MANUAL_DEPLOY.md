# 🖥️ Ручной деплой на ваш VPS сервер

## 📋 Данные сервера

- **IP**: 147.45.103.47
- **Логин**: node
- **Пароль**: j48i8L#+.@HJv.

---

## ⚠️ ВАЖНО

На сервере уже работает телеграм бот - **его НЕ ТРОГАЕМ!**
Новый бот установим в отдельную папку.

---

## 🚀 Способ 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

```bash
cd /Users/grigoryvinogradov/PSS2/PoketonSS
./deploy_to_server.sh
```

Скрипт попросит ввести пароль и задеплоит автоматически.

---

## 🖐️ Способ 2: Ручной деплой (Пошагово)

### Шаг 1: Подключитесь к серверу

```bash
ssh node@147.45.103.47
# Введите пароль: j48i8L#+.@HJv.
```

### Шаг 2: Проверьте что уже работает

```bash
# Смотрим существующие процессы Python
ps aux | grep python

# Смотрим какие боты запущены
ps aux | grep bot
```

**НЕ ОСТАНАВЛИВАЙТЕ существующие процессы!**

### Шаг 3: Создайте папку для нового бота

```bash
cd ~
mkdir -p PoketonSS
cd PoketonSS
```

### Шаг 4: Клонируйте репозиторий

```bash
git clone https://github.com/Vingrig-Studio/PoketonSS.git .
```

Если Git не установлен:
```bash
sudo apt update && sudo apt install -y git
```

### Шаг 5: Установите зависимости

```bash
# Проверьте Python
python3 --version

# Установите pip если нужно
sudo apt install -y python3-pip

# Установите зависимости бота
pip3 install -r requirements.txt
```

### Шаг 6: Создайте .env файл с токеном (ЗАЩИЩЕННО)

```bash
cd ~/PoketonSS

# Создайте .env файл
echo "TSSLG=8445125611:AAHnz_TPASOB4G1aNMCfGPekPGwZqoCFlxA" > .env

# Проверьте что файл создан
cat .env

# Установите права доступа (только владелец может читать)
chmod 600 .env
```

### Шаг 7: Запустите бота

```bash
# Запуск в фоновом режиме
nohup python3 bot.py > bot.log 2>&1 &

# Проверьте что бот запустился
ps aux | grep "python3 bot.py"

# Смотрите логи
tail -f bot.log
```

Вы должны увидеть:
```
✅ Токен загружен из файла .env (TSSLG)
INFO:__main__:Бот запущен и готов к работе...
```

### Шаг 8: Проверьте что оба бота работают

```bash
# Должно быть 2 процесса Python (старый бот + новый)
ps aux | grep python
```

### Шаг 9: Настройте автозапуск (опционально)

Создайте systemd service:

```bash
sudo nano /etc/systemd/system/poketon-bot.service
```

Вставьте:
```ini
[Unit]
Description=Poketon Sticker Shot Telegram Bot
After=network.target

[Service]
Type=simple
User=node
WorkingDirectory=/home/node/PoketonSS
ExecStart=/usr/bin/python3 /home/node/PoketonSS/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable poketon-bot
sudo systemctl start poketon-bot
sudo systemctl status poketon-bot
```

---

## 🔍 Проверка

### Проверить что бот работает:
```bash
ps aux | grep "python3 bot.py"
```

### Смотреть логи:
```bash
tail -f ~/PoketonSS/bot.log
```

### Остановить бота:
```bash
pkill -f "python3 bot.py"
# или
sudo systemctl stop poketon-bot
```

### Перезапустить:
```bash
cd ~/PoketonSS
nohup python3 bot.py > bot.log 2>&1 &
# или
sudo systemctl restart poketon-bot
```

---

## 🔒 Безопасность

✅ Токен хранится в `.env` файле на сервере  
✅ Права доступа `.env`: 600 (только владелец)  
✅ `.env` в `.gitignore` - не попадет в Git  
✅ Токен не виден в процессах  

---

## 📊 Структура на сервере

```
/home/node/
├── existing_bot/           # Старый бот (НЕ ТРОГАЕМ!)
│   └── ...
│
└── PoketonSS/             # Новый бот
    ├── bot.py
    ├── .env               # Токен здесь (защищен)
    ├── requirements.txt
    ├── TG.png
    ├── bot.log           # Логи
    └── ...
```

---

## 🆘 Проблемы

### Ошибка: "Permission denied"
```bash
sudo chown -R node:node ~/PoketonSS
chmod +x ~/PoketonSS/bot.py
```

### Ошибка: "Module not found"
```bash
pip3 install -r requirements.txt --user
```

### Порт занят
Боту не нужен открытый порт - он работает через Telegram API.

---

## ✅ Готово!

После выполнения всех шагов:
1. ✅ Старый бот работает
2. ✅ Новый бот работает
3. ✅ Токен защищен
4. ✅ Оба бота в разных папках

**Проверьте бота в Telegram командой `/start`!** 🎉

